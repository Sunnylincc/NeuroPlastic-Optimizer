experiment:
  dataset: mnist
  batch_size: 128
  epochs: 3
  lr: 0.001
  weight_decay: 0.01
  optimizer: adamw
  seed: 42
  num_workers: 2
  output_dir: results
  checkpoint_dir: checkpoints
  device: cpu
  scheduler: exponential
  scheduler_gamma: 0.97

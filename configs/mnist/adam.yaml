experiment:
  dataset: mnist
  run_name: adam
  batch_size: 128
  epochs: 3
  lr: 0.001
  weight_decay: 0.0
  optimizer: adam
  seed: 42
  num_workers: 2
  output_dir: results
  checkpoint_dir: checkpoints
  device: cpu
